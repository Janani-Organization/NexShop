# Shared application properties across all microservices
management.tracing.sampling.probability=1.0
management.zipkin.tracing.endpoint=${ZIPKIN_ENDPOINT:http://zipkin:9411/api/v2/spans}

# Force services to register with pod IP instead of unresolvable pod hostname
eureka.instance.prefer-ip-address=true
